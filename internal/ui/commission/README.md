# Objective UI

This package contains the user interface for managing objectives. It's currently in an incomplete state and needs to be updated to work with the revised objective package. We're skipping the implementation of this for now to get the codebase to build.

**TODO:**
- Update the UI to work with the PlanningSession type
- Implement the missing methods in the Planner
- Fix the generator interface usage